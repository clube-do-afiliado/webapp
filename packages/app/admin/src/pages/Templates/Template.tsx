import Page from '@cda/ui/layout/Page';

import { release } from '@/services/core';

export default function Template() {
    return (
        <Page
            title="Editar template de postagem"
            release={release}
        >

        </Page>
    );
}